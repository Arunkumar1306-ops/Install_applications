name: Event Types
 
on:
  push:
    branches:
      - "feature_*"
    # branches-ignore:
    #   - "feature_*"
    paths:
       - "src/**"
 
jobs:
  build:
    runs-on: ubuntu-latest
 
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
 
      - name: Setup Python
        run: echo "hello world"
 
