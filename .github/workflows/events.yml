name: Event Types
 
on:
  push:
    branches:
      - main
      - 'feature_1'


  workflow_dispatch:
 
jobs:
  build:
    runs-on: ubuntu-latest
 
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
 
      - name: Setup Python
        run: echo "hello world"
 
 
      - name: Run Python script
        run: python main.py
 
